namespace PopFlixBackend._2Application.Interfaces
{
    /// <summary>
    /// Defines a contract for managing user data storage and retrieval operations.
    /// </summary>
    /// <remarks>This interface provides methods for interacting with user data, such as creating, updating,
    /// deleting,  and retrieving user information. Implementations of this interface are responsible for handling the 
    /// underlying data storage mechanism (e.g., database, in-memory storage).</remarks>
    public interface IUserRepository
    {
    }
}
